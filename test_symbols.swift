import Foundation

let symbols = [
    "bell.badge.fill",
    "envelope.fill",
    "exclamationmark.bubble.fill",
    "hand.raised.fill",
    "doc.text.fill",
    "lock.shield.fill",
    "person.crop.circle.badge.checkmark.fill",
    "person.crop.circle.fill.badge.checkmark",
    "info.circle.fill",
    "doc.text.magnifyingglass",
    "person.crop.circle.badge.xmark.fill",
    "person.crop.circle.fill.badge.xmark",
    "rectangle.portrait.and.arrow.right.fill",
    "trash.fill"
]

// Just compile check to see if valid, wait actually SF symbols aren't checked at compile time for strings.
// There is no easy way to validate strings from CLI unless using CoreUI, but I'll just use the standard `.fill` patterns.
